#include <bits/stdc++.h> 
 #define ll long long int
 
using namespace std;
int main(){
    #ifndef ONLINE_JUDGE
freopen("input.txt", "r", stdin);
freopen("output.txt", "w", stdout);
#endif

    vector<ll> v;
    v.push_back(1);
    v.push_back(1);
    v.push_back(1);
    for(auto x: v)
        cout<<x<<" ";
    return 0;
}